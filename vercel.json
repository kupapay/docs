{
  "buildCommand": "python -m venv /tmp/venv && /tmp/venv/bin/pip install -r design/requirements.txt && cd design && /tmp/venv/bin/mkdocs build",
  "outputDirectory": "design/site",
  "framework": null
}
